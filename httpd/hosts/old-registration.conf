<VirtualHost *:443>
	ServerName			registration2.apsim.info
	ServerAdmin			julian.rich@csiro.au

	# Proxy to kestrel web server running in registration-site docker image.
	ProxyPreserveHost	On
	ProxyPass 			/							http://registration-site/
	ProxyPassReverse 	/							http://registration-site/

	ErrorLog			${APACHE_LOG_DIR}/registration-error.log
	CustomLog			${APACHE_LOG_DIR}/registration-access.log common

	SSLEngine On
	SSLProxyEngine On
	ProxyAddHeaders On
</VirtualHost>

# Listen on port 80 (ie http) but redirect to 443 (https).
<VirtualHost *:80>
	ServerName			registration2.apsim.info
	ServerAdmin			julian.rich@csiro.au

	ErrorLog			${APACHE_LOG_DIR}/registration2-error.log
	CustomLog			${APACHE_LOG_DIR}/registration2-access.log common

	RewriteEngine		On
	RewriteCond			%{SERVER_NAME} =registration2.apsim.info
	RewriteRule			^							https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
	ProxyAddHeaders On
</VirtualHost>